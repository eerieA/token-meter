# Some config constants for the app

# Centralized timing / policy for refreshes and cache
REFRESH_INTERVAL_MS = 3 * 60 * 1000  # 3 minutes for QTimer.start(...)
CACHE_TTL_SECONDS = 3 * 60  # 3 minutes for cache_valid(...)
